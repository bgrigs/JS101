// What will the following code output?
console.log(false == '0');
console.log(false === '0');

// Line 2 will log true
// Line 3 will log false

// The == operator coerces the values to the same type before comparing them.